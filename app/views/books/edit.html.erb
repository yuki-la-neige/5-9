<body>
	<main>
		<h2>Editing Book</h2>
		<%= form_for(@book) do |f| %>
			<% if @book.errors.any? %>
				<%= @book.errors.count %>errors prohibited this obj from being saved:<br/>
				<%= @book.errors.full_messages.each do |message| %>
					<%= message %>
				<% end %><br/>
			<% end %>

			<%= f.label :Title %><br/>
			<%= f.text_field :title %><br/>
			<%= f.label :Opinion %><br/>
			<%= f.text_area :body %><br/>
			<%= f.submit "Update Books", :class => "btn btn-primary" %>
		<% end %>
	</main>
</body>