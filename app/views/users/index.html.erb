<body>
	<main>
		<div class="container-fluid">
			<div class="row">
				<div class="col-md-3">
					<div class="user_info">
						<h3>User Info</h3>
						<table>
								<tr>
									<td>
									 <%= attachment_image_tag current_user, :profile_image, :fill, 60, 60, class: "img-circle pull-left profile-thumb", fallback: "no_image.jpg"%>
									</td>
								</tr>
								<tr>
									<th>Name:</th><td><%= current_user.name %></td>
								</tr>
								 <tr>
									<th>Introduction:</th><td><%= current_user.introduction %></td>
								</tr>
						</table>
						<%= link_to "", edit_user_path(current_user.id), :class => "btn btn-default glyphicon glyphicon-wrench col-md-12" %>

					</div><br/>

					<div class="new_book">
						<h3>New Book</h3>
						<%= form_for(@book) do |f| %>
							<% if @book.errors.any? %>
								<%= @book.errors.count %>errors prohibited this obj from being saved:<br/>
								<%= @book.errors.full_messages.each do |message| %>
									<%= message %>
								<% end %><br/>
							<% end %>

							<%= f.label :Title %><br/>
							<%= f.text_field :title %><br/>
							<%= f.label :Opinion, :class => "col-md-12" %><br/>
							<%= f.text_area :body, :class => "col-md-12" %><br/>
							<%=  f.submit "投稿", :class => "btn btn-primary col-md-12" %>
						<% end %>
					</div>
				</div>
				<div class="col-md-9">
					<h3>Users</h3>
					<table class="table table-striped">
						<thead>
							<th>Image</th>
							<th>Name</th>
							<th></th>
						</thead>
						<tbody>
							<% @users.each do |user| %>
								<tr>
									<th>
										<%= attachment_image_tag user, :profile_image, :fill, 40, 40, fallback: "no_image.jpg", size: "40x40", class: "img-circle pull-left profile-thumb" %>
									</th>
									<th><%= user.name %></th>
									<th><%= link_to "show", user_path(user.id) %></th>
								</tr>
							<% end %>
						</tbody>
					</table>
				</div>
			</div>
		</div>
	</main>
</body>